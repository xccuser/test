/**
 * 
 */
package com.xcc.mybatis;

import org.apache.ibatis.session.SqlSession;

/**
 *  Administrator
 *
 */
public class UpadeTest {

	public static void main(String[] args){
	/*	SqlSession session=FKSqlSessionFactory.getSqlSession();
		User user=session.selectOne("com.xcc.mybatis.UserMapper.selectUser",1);
		user.setName("tom");
		user.setAge(25);
		session.update("com.xcc.mybatis.UserMapper.modifyUser",user);
		session.commit();
		session.close();*/
	}
}
